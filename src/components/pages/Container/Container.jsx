import styles from './Container.module.scss';

const Container = () => {
  return (<div className={styles.Container}>
    <h1>TEST some test here is to check</h1>
  </div>);
};

export default Container;